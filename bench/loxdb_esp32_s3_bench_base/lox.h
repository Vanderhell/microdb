// SPDX-License-Identifier: MIT
// Arduino sketch compatibility shim.
// The base bench folder historically carried the public header as `loxdb.h`.
// The sketch includes `lox.h`, so include the real header here.
#pragma once

#include "loxdb.h"

